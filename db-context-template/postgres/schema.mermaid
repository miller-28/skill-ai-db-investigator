erDiagram
    %% ─────────────────────────────────────────────────────────────
    %% schema.mermaid — Incremental entity-relationship diagram
    %%
    %% Purpose : Visual map of discovered DB structure.
    %% Lifecycle: Starts as a stub. Each investigation session that
    %%            reveals new tables, columns, or FK relations should
    %%            add them here. Remove the stub comment once the
    %%            first real entities are added.
    %% ─────────────────────────────────────────────────────────────

    %% << no entities discovered yet >>
    %% Add entities as investigations reveal them, e.g.:
    %%
    %% users {
    %%     int  internal_id  PK
    %%     uuid id
    %%     uuid tenant_id    FK
    %% }
    %%
    %% users }o--|| tenants : "tenant_id"
